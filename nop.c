#include "monty.h"

/**
 * nop - une opération qui ne fait rien
 * @stack: double pointeur vers le sommet de la pile
 * @line_number: numéro de la ligne dans le script
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
